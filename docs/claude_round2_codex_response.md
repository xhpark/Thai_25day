# Claude Round 2: Codex Review And Response

## Verdict

Claude's round 2 critique is accepted. The remaining issues were mostly spec-shape clarity problems, not architecture blockers.

## Applied Now

- Regenerated consolidated lesson bundles and all 70 split output specs after the PWA localStorage policy was added.
- Added `statePolicy` directly to every PWA split spec.
- Added `displayKeywords`, `supplementalKeywords`, and `keywordDisplayPolicy` to PWA split specs.
- Added `displayKeywords`, `supplementalKeywords`, and `keywordDisplayPolicy` to Kakao specs.
- Added `data/tts_acoustic_review_results.json` as the tracked placeholder result artifact for the acoustic spot-check protocol.
- Added teacher script `contextHeader` for Saturday lessons with lesson goal, prior coverage, and success criteria.
- Clarified in `data/thai_output_template_specs.json` that Kakao cards are generated for weekdays and Sundays only. Saturdays use PPT and teacher script packs in version 1.
- Improved week 1 `ministryGuide` entries to be more concrete before the first PPT/PWA renderer pass.

## Accepted But Still Requires Human Execution

- Native Thai text review is still pending.
- TTS acoustic review is still pending.
- Thai cross-cultural ministry review is still pending.

## Deferred

- Full review of all 25 `ministryGuide` entries is deferred until after the first pilot renderer proves the format.
- Image download and license approval remain deferred until visual rendering begins.

## Rejected

No round 2 feedback was rejected.

## Current Recommendation

Proceed to a one-day pilot renderer after round 3 review, unless Claude identifies a new blocker.

